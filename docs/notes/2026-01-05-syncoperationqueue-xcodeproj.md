# SyncOperationQueue in Xcode project

- Added `SyncOperationQueue.swift` to the YouTrek Xcode project target so it builds with the app.
- This addresses the missing type error when compiling `SyncCoordinator`.
