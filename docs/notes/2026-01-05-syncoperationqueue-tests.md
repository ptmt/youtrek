# SyncOperationQueue tests and CI

- Added `SyncOperationQueueTests` to cover ordering, activity sink updates, and error recovery.
- Updated the macOS build workflow to run `xcodebuild test` before signing.
